#pragma once // Ensures that a file will not be included multiple times in the current project.

class Calculator
{
public:
    double Calculate(double x, char oper, double y); // Declaring function Calculate to run mathematical operations, a.k.a. prototype.
};
// Simple class, compiler will generate default constructor and destructor.
